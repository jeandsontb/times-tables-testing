<%- include('../partials/header.ejs') %>

  <div class="starter-template">
    <h1>Tabuada do número <%= num %> </h1>
    <table class="table table-striped table-bordered table-hover" style="width: 400px; margin-left: 32%;" >
      <% numbers.map(item => { %>
        <tr>
          <td><%= item.num  %> </td>
          <td> x </td>
          <td><%= item.i %></td>
          <td> = </td>
          <td><%= item.result %></td>
        </tr>
      <% }) %>
    </table>  

  </div>

<%- include('../partials/footer.ejs') %>